{
  "name": "sce_postgres_local",
  "type": "postgres",
  "comment": "Shape only. Create this credential in the n8n UI; values come from infra/.env.",
  "data": {
    "host": "postgres",
    "port": 5432,
    "database": "${POSTGRES_DB}",
    "user": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}",
    "ssl": "disable"
  }
}
