{
  "name": "sce_api_token",
  "type": "httpHeaderAuth",
  "comment": "Shape only. Bearer token for the capture API; value comes from CAPTURE_API_TOKEN.",
  "data": {
    "name": "Authorization",
    "value": "Bearer ${CAPTURE_API_TOKEN}"
  }
}
